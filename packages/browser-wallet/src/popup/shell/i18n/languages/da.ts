import type { Translations } from './types';

const translations: Translations = {
    translation: {
        mainLayout: {
            title: 'Concordium',
        },
        setup: {
            continue: 'Fortsæt',
        },
    },
};

export default translations;
