import type en from './en';

const t: typeof en = {
    save: 'Gem',
    url: {
        label: 'JSON-RPC endpoint',
        validation: {
            required: 'JSON-PRC endpoint er påkrævet',
        },
    },
};

export default t;
