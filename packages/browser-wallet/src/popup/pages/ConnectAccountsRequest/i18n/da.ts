import type en from './en';

const t: typeof en = {
    header: '{{ url }} vil gerne tilføjes til din allowlist',
    actions: {
        cancel: 'Cancel',
        connect: 'Connect',
    },
};

export default t;
